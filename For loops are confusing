# For loops
# they do this thing called iteration. iterating over a sequence
# Iteration is: a sequence of instructions or code being repeated until a specific end result is achieved.
numbers = [1,2,3,4,5]
for item in numbers:
    print(item)

#While loop that does that exact same thing as the for loop above

i = 0
while i < len(numbers):
    print(numbers [i])
    i += 1
